/**
 * Exercise 4A - All together!
 *
 * Write a `TodoList` function component that takes a list of todos
 * as `props.todos` having the shape {id: number, title: string, completed: boolean}
 *
 * The componenet should create the appropriate HTML
 * to display a checkbox on the left a the todo item on the right
 * in an unordered list.
 *
 * */

import React from "react";

function TodoList(props) {}

export default TodoList;
